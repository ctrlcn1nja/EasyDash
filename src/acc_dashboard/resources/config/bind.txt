# EasyDash key bindings
# Format:  action = KeyName
#
# How to use with a steering wheel:
#   1. Open your wheel's profiling software (Thrustmaster Control Panel,
#      Fanatec Wheel Base, Logitech G HUB, etc.)
#   2. Bind a wheel button to the keyboard key listed here (e.g. F5)
#   3. Make sure the EasyDash window is focused (second monitor is ideal)
#
# Supported key names:
#   F1-F12, A-Z, PageUp, PageDown, Home, End,
#   Insert, Delete, Space, Return, Up, Down, Left, Right

cycle_pace_mode = F5
 