# EasyDash key bindings
# Format:  action = KeyName
#
# Keyboard keys (work as global hotkeys — no window focus needed):
#   F1-F12, A-Z, PageUp, PageDown, Home, End,
#   Insert, Delete, Space, Return, Up, Down, Left, Right
#
# Joystick / steering wheel buttons (polled directly, no G HUB mapping needed):
#   JOY_BUTTON_N  where N matches the button number shown in your wheel software
#   Example: JOY_BUTTON_25 = Logitech G29 button 25

cycle_pace_mode = F5
 